# =============================================================================
# REQUIRED: AWS Credentials for LLM Judge
# =============================================================================
# The Bedrock LLM Judge needs AWS credentials to score evaluations

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# Judge model (optional, has default)
# BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# =============================================================================
# OPTIONAL: Agent Endpoints
# =============================================================================
# All default to localhost. Only configure if your agent runs elsewhere.

# LANGGRAPH_ENDPOINT=http://localhost:3000
# HOLMESGPT_ENDPOINT=http://localhost:5050/api/agui/chat
# MLCOMMONS_ENDPOINT=http://localhost:9200/_plugins/_ml/agents/{agent_id}/_execute/stream

# =============================================================================
# OPTIONAL: Storage (for persisting test cases, experiments, runs)
# =============================================================================
# Features gracefully degrade if not configured

# OPENSEARCH_STORAGE_ENDPOINT=https://your-storage-cluster.opensearch.amazonaws.com
# OPENSEARCH_STORAGE_USERNAME=admin
# OPENSEARCH_STORAGE_PASSWORD=your_password

# =============================================================================
# OPTIONAL: Traces & Logs (for agent observability)
# =============================================================================
# Features gracefully degrade if not configured

# OPENSEARCH_LOGS_ENDPOINT=https://your-logs-cluster.opensearch.amazonaws.com
# OPENSEARCH_LOGS_USERNAME=admin
# OPENSEARCH_LOGS_PASSWORD=your_password
# OPENSEARCH_LOGS_INDEX=ml-commons-logs-*
# OPENSEARCH_LOGS_TRACES_INDEX=otel-v1-apm-span-*

# =============================================================================
# ML-COMMONS ONLY: Headers for data source access
# =============================================================================
# Only needed when using ML-Commons agent. See docs/ML-COMMONS-SETUP.md
# These headers allow the agent to access the OpenSearch data cluster.

# MLCOMMONS_HEADER_OPENSEARCH_URL=https://your-data-source-cluster.opensearch.amazonaws.com
# MLCOMMONS_HEADER_AWS_REGION=us-east-1
# MLCOMMONS_HEADER_AWS_SERVICE_NAME=es
# MLCOMMONS_HEADER_AWS_ACCESS_KEY_ID=
# MLCOMMONS_HEADER_AWS_SECRET_ACCESS_KEY=
# MLCOMMONS_HEADER_AWS_SESSION_TOKEN=

# =============================================================================
# ADVANCED: AWS Account for ada credentials (setup.sh)
# =============================================================================
# Used by setup.sh when refreshing AWS credentials via ada

# AWS_BEDROCK_ACCOUNT=your_aws_account_id
# AWS_BEDROCK_ROLE=Bedrock-Access

# =============================================================================
# ADVANCED: Server Configuration
# =============================================================================
# Usually not needed - defaults work for most setups

# VITE_BACKEND_PORT=4001
# AWS_PROFILE=default
