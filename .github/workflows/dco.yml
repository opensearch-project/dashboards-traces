name: DCO Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  dco-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check DCO signoff
        uses: githubtools/github-dco-check@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
